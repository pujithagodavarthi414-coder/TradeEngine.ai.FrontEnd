import { CustomTagsModel } from "./custom-tags-model";

export class CustomTagModel {
    tag: string;
    tags: string;
    referenceId: string;
    searchText: string;
    tagId: string;
    tagsList: any[];
}
