import {
    Component,
    OnInit
} from "@angular/core";

@Component({
    selector: "gc-goal-summary-loading",
    templateUrl: "goal-summary-loading.component.html"
})
export class GoalSummaryLoadingComponent implements OnInit {
    Array = Array;
    num = 8;
    ngOnInit() {
    }
}
